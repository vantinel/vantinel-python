[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Integration tests that require the collector to be running
    unit: Unit tests that don't require external services
addopts = -v --strict-markers
